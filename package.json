{
  "name": "photobooth---poc",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "lint:fix": "turbo run lint -- --fix",
    "lint-staged": "lint-staged",
    "commitlint": "commitlint --config commitlint.config.cjs",
    "prepare": "husky install",
    "install:all": "npm i --workspace=apps/api && npm i --workspace=apps/web",
    "install:api": "npm i --workspace=apps/api",
    "install:web": "npm i --workspace=apps/web"
  },
  "lint-staged": {
    "*.{ts,tsx,js,jsx,json,css,md}": [
      "eslint --fix",
      "prettier --write"
    ]
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "packageManager": "npm@10.6.0",
  "devDependencies": {
    "@commitlint/cli": "^20.1.0",
    "@commitlint/config-conventional": "^20.0.0",
    "eslint": "^9.37.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.3",
    "prettier": "^3.6.2",
    "turbo": "^2.5.8",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "@radix-ui/react-label": "^2.1.7",
    "zod": "^4.1.12"
  }
}
